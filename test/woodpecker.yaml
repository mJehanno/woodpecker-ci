when:
  - event: [push, pull_request, pull_request_closed, tag, release, deployment, cron, manual]
    branch: '**'

steps:
  - name: build
    image: debian
    commands:
      - echo "This is the build step"
  - name: a-test-step
    image: debian
    commands:
      - echo "Testing.."